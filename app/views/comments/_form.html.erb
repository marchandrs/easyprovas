<%= form_for [@post, @comment] do |f| %>
  <div class="field">
    <%= f.label "Comentário" %><br>
    <%= f.text_area :text, value: @comment.text %>
  </div>

  <div class="actions">
    <%= f.submit 'Salvar' %>
  </div>
<% end %>
