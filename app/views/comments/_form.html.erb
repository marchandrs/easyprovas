<%= form_for [@post, @post.comments.build] do |f| %>
  <div class="field">
    <%= f.label "Comentário" %><br>
    <%= f.text_area :text %>
  </div>

  <div class="actions">
    <%= f.submit 'Salvar' %>
  </div>
<% end %>
